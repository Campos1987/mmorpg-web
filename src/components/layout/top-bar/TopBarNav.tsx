/**
 * Slot central da navegação — conteúdo implementado na Fase 4.
 */
export function TopBarNav() {
  return (
    <nav
      className="hidden flex-1 justify-center lg:flex"
      aria-label="Navegação principal"
    />
  );
}
